A.
1.Writing, completeing and executing a c program in linux
    create a .c file on text editor line nano, vi or gedit
    say for nano
    nano test_program.c
        #include <stdio.h>
        void main(){
        printf("Hello World /n");
    }
    to save hit Ctrl + X, verify by entering "Y"
2.Compile the programme we use gcc (GNU compiler collection) to compile the program
    gcc test_program.c -o test_program
3.Run the program
        ./test_program
B.
1.Write the program
    nano test_program_2.c
        int a,b,sum;
          printf("enter two number\n");
          scanf("%d %d", &a, &b);
          sum=a+b;
          printf("sum=%d",sum);
          printf("\nsean\n");
        }
2.Compile the programme we use gcc (GNU compiler collection) to compile the program
    gcc test_program_2.c -o test_program_2
3.Run the program
        ./test_program_2
C.
1.Write the program
    nano test_program_3.c
       #include<stdio.h>
        void main( )
        {
          int a;
          float b, sum;
          printf("enter two number\n");
          scanf("%d %f", &a, &b);
          sum=a+b;
          printf("sum=%f",sum);
          printf("\nsean\n");
        }
2.Compile the programme we use gcc (GNU compiler collection) to compile the program
    gcc test_program_3.c -o test_program_3
3.Run the program
        ./test_program_3
